services:
  backend:
    build: .
    container_name: simulacron8n-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend:/app/backend
    restart: unless-stopped
